
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/erupt_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  jpa:
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
server:
  port: 8111
  servlet:
    session:
      timeout: 10800 # 设置会话超时时间为180分钟

logging:
  level:
    root: WARN